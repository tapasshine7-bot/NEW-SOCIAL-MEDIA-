import { AppShell } from "@/components/AppShell";
import { PostComposer } from "@/components/PostComposer";
import { Button } from "@/components/ui/button";
import { Clapperboard, ImagePlus, Sparkles } from "lucide-react";
import { Link, useLocation } from "wouter";

export default function Create() { const [location, navigate] = useLocation(); const params = new URLSearchParams(location.split("?")[1]); const mode = params.get("type") ?? "choose"; if (mode === "post") return <AppShell><section className="mx-auto max-w-2xl"><Link href="/create" className="text-sm font-medium text-muted-foreground hover:text-foreground">← Back to creation</Link><h1 className="mt-4 text-3xl font-semibold tracking-tight">New post</h1><p className="mt-2 text-sm text-muted-foreground">Choose media, add context, then publish when it feels ready.</p><div className="mt-6"><PostComposer defaultOpen onPublished={() => navigate("/", { replace: true })} /></div></section></AppShell>; return <AppShell><section className="mx-auto max-w-3xl"><p className="eyebrow">Create</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">What do you want to share?</h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">Choose a format. Each choice opens a focused creation experience with clear next steps.</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><CreateCard href="/create?type=post" icon={ImagePlus} title="New post" text="Share photos or videos with a caption." /><CreateCard href="/stories" icon={Sparkles} title="Story" text="Share a moment that disappears after a day." /><CreateCard href="/reels" icon={Clapperboard} title="Short video" text="Publish a vertical video for discovery." /></div><Link href="/" className="mt-7 inline-flex"><Button variant="ghost" className="rounded-full">Cancel</Button></Link></section></AppShell>; }
function CreateCard({ href, icon: Icon, title, text }: { href: string; icon: typeof ImagePlus; title: string; text: string }) { return <Link href={href} className="surface-panel group block p-5 transition hover:-translate-y-1 hover:border-primary/35"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-primary/10 text-primary transition group-hover:bg-primary group-hover:text-primary-foreground"><Icon size={21} /></span><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p><span className="mt-5 block text-sm font-semibold text-primary">Create {title.toLowerCase()} →</span></Link>; }
